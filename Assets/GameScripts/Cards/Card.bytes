local DataAllDict = {}
local DataAllArr = {}
local CardArcomage = require("Data/CardArcomage")

local function AddAll(data)
    for _, res in pairs(data) do
        if DataAllDict[res.id] == nil then
            DataAllDict[res.id] = res
            table.insert(DataAllArr, res)
        else
            warning("逻辑问题", string.format("卡数据中添加了重复的id:%s", res.id))
        end
    end
end

AddAll(CardArcomage)

return DataAllArr
